{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: n8n-sso-proxy
spec:
  selector:
    app: n8n-sso-proxy-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
{{- end }}
